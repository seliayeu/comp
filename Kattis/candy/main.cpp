#include <iostream>
#include <vector>
#include <unordered_map>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int N, F;
    std::cin >> N >> F;
    unsigned long long T;
    std::cin >> T;

    unsigned long long total{ 0 };
    std::vector<int> a(N);
    std::unordered_map<__uint128_t, int> numSwaps;

    for (int i = 0; i < N; ++i) {
    }
}
